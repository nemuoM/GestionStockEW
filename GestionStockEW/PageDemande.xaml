<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GestionStockEW.PageDemande"
             Title="Les demandes"
             Loaded="ContentPage_Loaded">
    <!--Margin : Gauche, haut, droite, bas -->
    <VerticalStackLayout>
        <CollectionView x:Name="colViewDemande" SelectionMode="Single" HorizontalOptions="Center" SelectionChanged="colViewDemande_SelectionChanged"
                Margin="200,10,0,0" MaximumHeightRequest="900">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="auto"/>
                        </Grid.ColumnDefinitions>
                        <Label Grid.Column="0" Grid.Row="0" Text="{Binding Affichage}" FontFamily="GSKPrecision" Margin="20,20,0,0" FontSize="30"/>
                    </Grid>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>